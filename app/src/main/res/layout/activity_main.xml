<?xml version="1.0" encoding="utf-8"?>
<!--
    Layout principal du Chronomètre Service.
    Centré verticalement avec un espacement cohérent entre les éléments.
    Garde les mêmes IDs (tvTemps, btnStart, btnStop) pour la compatibilité
    avec MainActivity.java.
-->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:gravity="center"
    android:padding="32dp"
    android:background="#F5F5F5">

    <!-- Titre de l'application -->
    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="⏱ Chronomètre"
        android:textSize="26sp"
        android:textStyle="bold"
        android:textColor="#333333"
        android:layout_marginBottom="8dp" />

    <!-- Sous-titre explicatif -->
    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Foreground Service actif"
        android:textSize="13sp"
        android:textColor="#888888"
        android:layout_marginBottom="40dp" />

    <!-- Affichage du temps en MM:SS -->
    <TextView
        android:id="@+id/tvTemps"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="00:00"
        android:textSize="72sp"
        android:textStyle="bold"
        android:textColor="#1A1A2E"
        android:fontFamily="monospace"
        android:layout_marginBottom="48dp" />

    <!-- Bouton Démarrer -->
    <Button
        android:id="@+id/btnStart"
        android:layout_width="220dp"
        android:layout_height="52dp"
        android:text="▶  Démarrer"
        android:textSize="16sp"
        android:textColor="#FFFFFF"
        android:backgroundTint="#4CAF50"
        android:layout_marginBottom="16dp" />

    <!-- Bouton Arrêter -->
    <Button
        android:id="@+id/btnStop"
        android:layout_width="220dp"
        android:layout_height="52dp"
        android:text="■  Arrêter"
        android:textSize="16sp"
        android:textColor="#FFFFFF"
        android:backgroundTint="#F44336" />

</LinearLayout>